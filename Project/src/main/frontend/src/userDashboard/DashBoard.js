import * as React from "react";

class DashBoard extends React.Component {
    render() {
        return (
            <div>
                C'est le DashBoard : contient le "addTools, editProfil, editToolz..."
            </div>
        );
    }
}

export default DashBoard;